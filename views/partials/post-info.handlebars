<section class ="p-2">
    <div class="card">
        <div class="card-header bg-success">
           <h4><a class="text-decoration-none text-white" href="/post/{{id}}">{{title}}</a></h4>
            <p>Posted by {{user.username}} on {{format_date created_at}}</p>
        </div>
            <p class="card-body">{{content}}</p>
    </div>
</section>